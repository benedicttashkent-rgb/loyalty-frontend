# 🔍 Проверка регистрации

## Проблема:
Регистрация не работает, хотя `VITE_API_BASE_URL` установлен в Vercel.

## ✅ Проверка:

### 1. Проверьте Vercel Environment Variables:

**Vercel Dashboard → Project → Settings → Environment Variables:**

Должна быть:
```
VITE_API_BASE_URL=https://web-production-9dbea.up.railway.app/api
```

**Важно:**
- URL должен быть полным: `https://...`
- Должен заканчиваться на `/api`
- НЕ должно быть слеша в конце: `https://...railway.app/api` (правильно)
- НЕ должно быть: `https://...railway.app/api/` (неправильно)

### 2. Проверьте в браузере DevTools:

1. Откройте ваше Vercel приложение
2. Откройте **DevTools (F12)** → **Console**
3. Введите:
   ```javascript
   console.log('API URL:', import.meta.env.VITE_API_BASE_URL);
   ```
4. Должно показать: `https://web-production-9dbea.up.railway.app/api`

### 3. Проверьте Network запросы:

1. **DevTools (F12)** → **Network** tab
2. Попробуйте зарегистрироваться
3. Найдите запрос к `send-otp`
4. Проверьте:
   - **Request URL** - должен быть `https://web-production-9dbea.up.railway.app/api/auth/send-otp`
   - **Status** - должен быть `200` или `400` (не `404` или `CORS error`)
   - **Response** - должен быть JSON

### 4. Проверьте CORS:

Если видите ошибку `CORS policy` в консоли:

**Railway Dashboard → BACKEND Service → Variables:**

Добавьте:
```
FRONTEND_URL=https://loyalty-frontend-six.vercel.app
```

---

## 🔍 Типичные проблемы:

### Проблема 1: VITE_API_BASE_URL не применяется
**Симптом:** В консоли `import.meta.env.VITE_API_BASE_URL` = `undefined`

**Решение:** 
- Перезапустите Vercel deployment
- Убедитесь, что переменная добавлена для **Production** environment

### Проблема 2: Неправильный URL
**Симптом:** 404 или CORS error

**Решение:** 
- Проверьте, что URL правильный
- Проверьте, что заканчивается на `/api` (без слеша)

### Проблема 3: Backend недоступен
**Симптом:** Network Error или Timeout

**Решение:** 
- Проверьте, что Railway backend запущен
- Откройте `https://web-production-9dbea.up.railway.app/health`

---

## ✅ Быстрая проверка:

1. **В браузере Console:**
   ```javascript
   console.log(import.meta.env.VITE_API_BASE_URL);
   ```
   Должно показать ваш Railway URL

2. **В Network tab:**
   - Запрос должен идти на Railway, не на Vercel

3. **Проверьте Railway:**
   - Backend должен быть запущен
   - `/health` должен возвращать JSON

---

✅ **После проверки всех пунктов регистрация должна заработать!**
