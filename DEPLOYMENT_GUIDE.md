# 🚀 Детальный план деплоя Benedict Cafe Loyalty

## ✅ Проверка готовности

### Frontend (готов ✅)
- ✅ `src/config/api.js` - правильная логика с `VITE_API_BASE_URL` и fallback
- ✅ `package.json` - скрипты сборки настроены
- ✅ `vite.config.mjs` - конфигурация Vite корректна
- ✅ Нет синтаксических ошибок
- ✅ Репозиторий: `https://github.com/benedicttashkent-rgb/loyalty-frontend.git`

### Backend (готов ✅)
- ✅ `package.json` - скрипты настроены:
  - `npm start` → запускает бота (`start-bot.js`)
  - `npm run start:api` → запускает API (`src/index.js`)
- ✅ `start-bot.js` - точка входа для бота
- ✅ `src/index.js` - точка входа для API
- ✅ Нет синтаксических ошибок
- ✅ Репозиторий: `https://github.com/azamoviich/loyaltyback.git`

---

## 📋 ЧАСТЬ 1: Деплой Backend на Railway

### Шаг 1.1: Создание проекта Railway

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий: `azamoviich/loyaltyback`
5. Railway автоматически определит проект и начнет деплой

### Шаг 1.2: Настройка переменных окружения (Backend)

В Railway Dashboard → Ваш проект → **Variables** → добавьте:

#### 🔴 ОБЯЗАТЕЛЬНЫЕ переменные:

```env
# Database
DATABASE_URL=postgresql://user:password@host:port/database

# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_WEB_APP_URL=https://ваш-frontend.vercel.app/

# iiko API
IIKO_API_LOGIN=ваш_логин
IIKO_API_PASSWORD=ваш_пароль
IIKO_ORGANIZATION_ID=ваш_organization_id

# JWT & Security
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа
SESSION_SECRET=ваш_секретный_ключ_для_сессий

# Admin Panel
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_безопасный_пароль

# Environment
NODE_ENV=production
PORT=8080
```

#### 🟡 ОПЦИОНАЛЬНЫЕ переменные:

```env
# Frontend URL (для CORS)
FRONTEND_URL=https://ваш-frontend.vercel.app

# ESKIZ SMS (если используется)
ESKIZ_EMAIL=ваш_email
ESKIZ_PASSWORD=ваш_пароль

# Auto Sync (если нужна автоматическая синхронизация с iiko)
AUTO_SYNC_ENABLED=true
AUTO_SYNC_SCHEDULE=0 2 * * *  # Каждый день в 2:00
AUTO_CHECK_SCHEDULE=0 */6 * * *  # Каждые 6 часов

# Telegram Chat ID для уведомлений о заказах
TELEGRAM_TAKEAWAY_CHAT_ID=ваш_chat_id
```

### Шаг 1.3: Создание двух сервисов на Railway

#### Сервис 1: API Service

1. В Railway Dashboard → Ваш проект → **"+ New"** → **"Service"**
2. Выберите тот же репозиторий `azamoviich/loyaltyback`
3. Назовите сервис: **"loyalty-api"** (или "backend-api")
4. Перейдите в **Settings** → **Deploy**:
   - **Start Command**: `npm run start:api`
5. Убедитесь, что все переменные окружения добавлены (они наследуются от проекта)

#### Сервис 2: Bot Service

1. В том же проекте → **"+ New"** → **"Service"**
2. Выберите тот же репозиторий `azamoviich/loyaltyback`
3. Назовите сервис: **"telegram-bot"** (или "tgbot")
4. Перейдите в **Settings** → **Deploy**:
   - **Start Command**: `npm start`
5. Убедитесь, что все переменные окружения добавлены

### Шаг 1.4: Настройка базы данных

1. В Railway Dashboard → Ваш проект → **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway создаст PostgreSQL базу данных
3. Скопируйте **DATABASE_URL** из настроек базы данных
4. Добавьте `DATABASE_URL` в переменные окружения обоих сервисов (API и Bot)

### Шаг 1.5: Запуск миграций

1. В Railway Dashboard → API Service → **Deployments**
2. Найдите последний деплой → **"..."** → **"View Logs"**
3. Проверьте, что сервис запустился
4. Откройте **Settings** → **Deploy** → **"Run Command"**
5. Выполните: `npm run migrate`
6. Проверьте логи - должны быть сообщения об успешных миграциях

### Шаг 1.6: Проверка работы Backend

1. В Railway Dashboard → API Service → **Settings** → **Networking**
2. Скопируйте **Public Domain** (например: `https://web-production-xxxxx.up.railway.app`)
3. Откройте в браузере: `https://ваш-domain.up.railway.app/health`
4. Должен вернуться JSON с `status: "ok"`

**✅ Backend готов!** Сохраните этот URL - он понадобится для Frontend.

---

## 📋 ЧАСТЬ 2: Деплой Frontend на Vercel

### Шаг 2.1: Создание проекта Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите **"Import Git Repository"**
4. Найдите и выберите: `benedicttashkent-rgb/loyalty-frontend`
5. Нажмите **"Import"**

### Шаг 2.2: Настройка проекта Vercel

1. **Framework Preset**: Vercel должен автоматически определить **Vite**
2. **Root Directory**: оставьте пустым (или `./`)
3. **Build Command**: `npm run build` (должно быть по умолчанию)
4. **Output Directory**: `build` (должно быть по умолчанию)
5. **Install Command**: `npm install` (должно быть по умолчанию)

### Шаг 2.3: Настройка переменных окружения (Frontend)

В Vercel Dashboard → Ваш проект → **Settings** → **Environment Variables** → добавьте:

```env
VITE_API_BASE_URL=https://ваш-railway-domain.up.railway.app/api
```

**⚠️ ВАЖНО:** 
- URL должен заканчиваться на `/api`
- Используйте **Public Domain** из Railway API Service
- Пример: `https://web-production-9dbea.up.railway.app/api`

### Шаг 2.4: Деплой Frontend

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя Vercel покажет **Production URL**
4. Скопируйте этот URL (например: `https://loyalty-frontend.vercel.app`)

### Шаг 2.5: Обновление Backend переменных

Вернитесь в Railway Dashboard → обновите переменные окружения:

```env
# В API Service и Bot Service обновите:
FRONTEND_URL=https://ваш-frontend.vercel.app
TELEGRAM_WEB_APP_URL=https://ваш-frontend.vercel.app/
```

### Шаг 2.6: Проверка работы Frontend

1. Откройте Production URL в браузере
2. Откройте **Developer Tools** (F12) → **Console**
3. Проверьте логи - должны быть сообщения:
   ```
   🔍 Signup Page - API Configuration:
      VITE_API_BASE_URL: https://ваш-railway-domain.up.railway.app/api
      API Base URL: https://ваш-railway-domain.up.railway.app/api
   ```
4. Попробуйте зарегистрироваться - должно работать

**✅ Frontend готов!**

---

## 📋 ЧАСТЬ 3: Финальная проверка

### 3.1: Проверка Backend API

```bash
# Health check
curl https://ваш-railway-domain.up.railway.app/health

# Должен вернуться:
{
  "status": "ok",
  "timestamp": "...",
  "environment": "production",
  ...
}
```

### 3.2: Проверка Telegram Bot

1. Откройте Telegram → найдите вашего бота
2. Отправьте `/start`
3. Бот должен ответить с кнопкой "Открыть приложение"
4. Кнопка должна открывать ваш Frontend на Vercel

### 3.3: Проверка Frontend → Backend связи

1. Откройте Frontend в браузере
2. Откройте **Developer Tools** (F12) → **Network**
3. Попробуйте зарегистрироваться
4. Проверьте запросы - они должны идти на Railway URL, а не на Vercel

### 3.4: Проверка базы данных

1. В Railway Dashboard → Database → **"Query"**
2. Выполните: `SELECT COUNT(*) FROM customers;`
3. Должна вернуться таблица с количеством пользователей

---

## 🔧 Решение проблем

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте `VITE_API_BASE_URL` в Vercel - должен быть полный URL с `/api`
2. Проверьте CORS в Railway - убедитесь, что `FRONTEND_URL` добавлен в переменные
3. Проверьте логи Railway API Service - должны быть запросы от Frontend

### Проблема: Telegram Bot не отвечает

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` в Railway Bot Service
2. Проверьте логи Bot Service - должны быть сообщения о запуске
3. Убедитесь, что Bot Service запущен (Status: Running)

### Проблема: 401/403 ошибки

**Решение:**
1. Проверьте `JWT_SECRET` и `SESSION_SECRET` - должны быть установлены
2. Проверьте `ADMIN_USERNAME` и `ADMIN_PASSWORD` для админ-панели
3. Убедитесь, что переменные добавлены в оба сервиса (API и Bot)

### Проблема: База данных не работает

**Решение:**
1. Проверьте `DATABASE_URL` - должен быть правильный формат PostgreSQL
2. Запустите миграции: `npm run migrate` в Railway API Service
3. Проверьте логи API Service - должны быть сообщения о подключении к БД

---

## 📝 Чеклист деплоя

### Backend (Railway)
- [ ] Проект создан
- [ ] PostgreSQL база данных добавлена
- [ ] API Service создан с `npm run start:api`
- [ ] Bot Service создан с `npm start`
- [ ] Все переменные окружения добавлены
- [ ] Миграции выполнены (`npm run migrate`)
- [ ] Health check работает (`/health`)
- [ ] Public Domain скопирован

### Frontend (Vercel)
- [ ] Проект создан и подключен к GitHub
- [ ] `VITE_API_BASE_URL` установлен (Railway URL + `/api`)
- [ ] Деплой успешен
- [ ] Production URL скопирован

### Финальная настройка
- [ ] `FRONTEND_URL` обновлен в Railway
- [ ] `TELEGRAM_WEB_APP_URL` обновлен в Railway
- [ ] Telegram Bot работает
- [ ] Frontend подключается к Backend
- [ ] Регистрация работает
- [ ] Админ-панель доступна

---

## 🎯 Итоговые URL

После деплоя у вас будут:

- **Backend API**: `https://ваш-railway-domain.up.railway.app`
- **Frontend**: `https://ваш-frontend.vercel.app`
- **Telegram Bot**: работает через Bot Service на Railway

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Railway (API Service и Bot Service)
2. Проверьте логи в Vercel (Deployments → View Logs)
3. Проверьте переменные окружения в обоих сервисах
4. Убедитесь, что все URL правильные и доступны

**Удачи с деплоем! 🚀**
